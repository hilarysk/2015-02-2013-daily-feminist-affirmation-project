<% @array.each do |object|  %>
    <%= erb :"_#{object.id}", :locals => {:color => hash.values[0]}%> 
    <% end %>
  
  
  
  
  get find_specific_record to return array of objects, then filter through here, use attr reader methods to pull specific attributes, then format in the .erb file for that 
  
  